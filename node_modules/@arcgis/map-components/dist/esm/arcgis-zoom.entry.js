/*!
 * All material copyright Esri, All Rights Reserved, unless otherwise specified.
 * See https://js.arcgis.com/4.30/esri/copyright.txt for details.
 * v4.30.0-next.37
 */
import { r as registerInstance, c as createEvent, h, g as getElement } from './index-6e172aac.js';
import { a7 as newWidgetsZoom } from './index-0bc55ca5.js';
import { r as referenceElementHelper, c as checkForView } from './component-utils-23194f7c.js';
import { o as onDisconnectedHelper } from './disconnected-6cf1c100.js';

const zoomCss = "arcgis-zoom{}";

const ArcgisZoom = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
        this.arcgisZoomReady = createEvent(this, "arcgisZoomReady", 7);
        this._watchHandles = [];
        this.referenceElement = undefined;
        this.icon = undefined;
        this.layout = "vertical";
        this.position = "bottom-left";
        this.view = undefined;
        this.widget = undefined;
    }
    referenceElementWatcher(value) {
        void referenceElementHelper(this, value);
    }
    iconWatcher(value) {
        if (this.widget && this.widget.icon !== value) {
            this.widget.icon = value;
        }
    }
    layoutWatcher(value) {
        if (this.widget && this.widget.layout !== value) {
            this.widget.layout = value;
        }
    }
    positionWatcher(value) {
        if (value !== undefined && this.widget) {
            this.view.ui.move(this.childElem, value);
        }
    }
    viewWatcher(value) {
        if (value !== undefined && this.widget) {
            this.widget.view = value;
        }
    }
    //--------------------------------------------------------------------------
    //
    //  Methods
    //
    //--------------------------------------------------------------------------
    async zoomIn() {
        this.widget?.viewModel.zoomIn();
    }
    async zoomOut() {
        this.widget?.viewModel.zoomOut();
    }
    //--------------------------------------------------------------------------
    //
    //  Lifecycle
    //
    //--------------------------------------------------------------------------
    async componentDidLoad() {
        const params = this._createParams();
        const widget = await newWidgetsZoom(params);
        this.widget = widget;
        this._addContainer();
        this.arcgisZoomReady.emit();
        checkForView(this);
    }
    disconnectedCallback() {
        onDisconnectedHelper(this);
    }
    render() {
        return h("div", { ref: (el) => (this.childElem = el) });
    }
    //--------------------------------------------------------------------------
    //
    //  Private Methods
    //
    //--------------------------------------------------------------------------
    _addContainer() {
        this.widget.container = this.childElem;
    }
    _createParams() {
        const params = {};
        if (this.icon) {
            params.icon = this.icon;
        }
        if (this.layout) {
            params.layout = this.layout;
        }
        if (this.view) {
            params.view = this.view;
        }
        return params;
    }
    get el() { return getElement(this); }
    static get watchers() { return {
        "referenceElement": ["referenceElementWatcher"],
        "icon": ["iconWatcher"],
        "layout": ["layoutWatcher"],
        "position": ["positionWatcher"],
        "view": ["viewWatcher"]
    }; }
};
ArcgisZoom.style = zoomCss;

export { ArcgisZoom as arcgis_zoom };
